{
  "CREATE": {
    "/SchemaHandlerPackage": "{{HookPackage}}",
    "/TypeName": "{{HookName}}",
    "/ExecutionRoleArn": "{{ExecutionRoleArn}}",
    "/LoggingConfig": {
      "LogGroupName": "{{LogGroupName}}",
      "LogRoleArn": "{{LogRoleArn}}"
    }
  }
}
